import { View } from 'react-native'
import React from 'react'
import { vScale } from '../utils/dimension'

const ItemSeparator = () => {
  return (
    <View style={{ height: vScale(20) }}>
    </View>
  )
}

export default ItemSeparator